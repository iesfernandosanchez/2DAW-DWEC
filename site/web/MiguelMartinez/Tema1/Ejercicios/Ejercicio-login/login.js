function verificarDatos(usuario, contraseña) {
    if (usuario == "Gwenevere" && contraseña == "excalibur") {
        return "Usuario y contraseña correctos";
    } else {
        return "Usuario y contraseña incorrectos";
    }
}