function comprobar(usuario, clave){
    if (usuario=="Gwenevere" && clave=="excalibur"){
        prompt("Usuario y contraseña correctos. Puede ingresar a Camelot")
    }else{
        prompt("Acceso denegado")
    }
}
